# URL del backend (Express). En desarrollo local no hace falta tocarlo.
# Cuando despliegues el backend (Render, Railway, etc.), cambiá este valor
# por la URL pública, por ejemplo: https://nr-rada-backend.onrender.com
VITE_API_URL=http://localhost:4000
